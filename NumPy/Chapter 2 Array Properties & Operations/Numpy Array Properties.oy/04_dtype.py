'''
dtype ->  Data Type of the elements in the array
For example, an array can contain integers, floats, or strings.
'''

import numpy as np

arr = np.array([1, 2, 3, 4, 5])
arr2 = np.array([1, 2.3, 3.2, 4, 5])
print(arr.dtype)
print(arr2.dtype)